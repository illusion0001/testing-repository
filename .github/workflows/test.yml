name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - run: |
          echo $(git log --oneline)$'\n<details>'$'\n<summary>Changelist</summary>'$'\n\`\`\`''\`\`\`'$'\n''</details>' > test5.md
          cat test5.md
