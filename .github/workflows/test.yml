name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - run: |
          git rev-list HEAD --count
          chmod +x *.sh
          ./cl.sh

      - uses: actions/upload-artifact@v3
        with:
          path: cl.md
          retention-days: 1
