name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - run: |
          #echo Changelist: >> test.md
          #git log --oneline >> test.md
          #cat test.md
          cat test.md test2.md >> test3.md
          cat test3.md

