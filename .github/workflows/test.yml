name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - run: |
          echo Changelist: >> test.txt
          git log --oneline >> test.txt
          cat test.txt
          echo ugh
